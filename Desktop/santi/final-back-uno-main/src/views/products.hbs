<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Productos | Tradición Mate</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <h1>Todos los productos</h1>

    <div class="product-grid">
        {{#each products}}
        <div class="product-card">
            <h3>{{title}}</h3>
            <p>Precio: ${{price}}</p>
            <a href="/products/{{_id}}">Ver detalle</a>
        </div>
        {{/each}}
    </div>

    <div class="pagination">
        {{#if hasPrevPage}}<a href="/products?page={{prevPage}}">⬅ Anterior</a>{{/if}}
        <span>Página {{page}} de {{totalPages}}</span>
        {{#if hasNextPage}}<a href="/products?page={{nextPage}}">Siguiente ➡</a>{{/if}}
    </div>
</body>
</html>
